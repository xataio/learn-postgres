title: Full-text search
summary: Build search with tsvector and tsquery — tokenization, stemming, ranking, and GIN indexes.
estimatedMinutes: 14
tags:
  - full-text-search
  - tsvector
  - tsquery
  - ranking
  - gin-index
authors:
  - exekias
seed: seed.sql
checks:
  - id: search-index-created
    type: query-returns
    description: Create a search-optimized column and GIN index on the articles table.
    sql: >
      SELECT indexname FROM pg_indexes
      WHERE tablename = 'articles' AND indexname = 'idx_articles_search'
    expect:
      rowCount: 1
      rows:
        - [idx_articles_search]
