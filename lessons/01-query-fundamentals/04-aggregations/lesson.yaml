title: Aggregations and GROUP BY
summary: Roll rows up — COUNT, SUM, AVG, GROUP BY, and HAVING.
estimatedMinutes: 12
tags:
  - aggregate
  - group-by
  - having
authors:
  - exekias
seed: seed.sql
checks:
  - id: seed-loaded
    type: row-count
    description: The seeded orders table has 15 rows — click to mark this lesson done.
    table: orders
    expect:
      rowCount: 15
