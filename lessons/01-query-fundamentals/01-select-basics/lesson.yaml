title: SELECT basics
summary: Pull rows out of a table — projection, filtering, and ordering.
difficulty: beginner
estimatedMinutes: 8
tags:
  - select
  - where
  - order-by
authors:
  - exekias
seed: seed.sql
checks:
  - id: seed-loaded
    type: row-count
    description: The seeded users table has 10 rows — click to mark this lesson done.
    table: users
    expect:
      rowCount: 10
