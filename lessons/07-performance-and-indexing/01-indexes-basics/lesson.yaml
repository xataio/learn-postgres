title: "Indexes: the basics"
summary: How a B-tree index turns a full-table scan into a targeted lookup — and when it won't help.
estimatedMinutes: 15
tags:
  - indexes
  - b-tree
  - create-index
  - explain
  - selectivity
authors:
  - exekias
seed: seed.sql
checks:
  - id: user-id-index-created
    type: query-returns
    description: Create the B-tree index on events(user_id).
    sql: "SELECT count(*) FROM pg_indexes WHERE tablename = 'events' AND indexdef ILIKE '%(user_id)%'"
    expect:
      rowCount: 1
      rows:
        - [1]
