title: Reading EXPLAIN
summary: Read the planner's query plan — the cost estimates, the tree of nodes, and what EXPLAIN ANALYZE adds by actually running the query.
estimatedMinutes: 15
tags:
  - explain
  - explain-analyze
  - query-plan
  - performance
  - buffers
authors:
  - exekias
seed: seed.sql
checks:
  - id: status-index-created
    type: query-returns
    description: Add the orders(status) index your EXPLAIN plan revealed was missing.
    sql: "SELECT count(*) FROM pg_indexes WHERE tablename = 'orders' AND indexdef ILIKE '%(status)%'"
    expect:
      rowCount: 1
      rows:
        - [1]
